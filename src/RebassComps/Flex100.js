// /src/RebassComps
import { Flex as FlexBase } from "rebass";
import { borders, height, minHeight } from "styled-system";
import styled from "styled-components";

export const Flex100 = styled(FlexBase)`
  ${borders}
  ${height}
  ${minHeight}
`;
